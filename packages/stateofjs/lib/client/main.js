import '../modules/index.js';

// disable hot code reload to avoid messing with people's half-filled forms

// eslint-disable-next-line
Reload._onMigrate(function() {
  return [false];
})

const foo = 123234098089890;